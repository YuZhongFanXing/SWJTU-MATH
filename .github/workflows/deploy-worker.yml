name: Deploy Upload Worker

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      HAS_APP_CONFIG: ${{ secrets.OAUTH_CLIENT_ID }}
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Deploy Worker code
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: worker
          wranglerVersion: "4.50.0"
      - name: Configure application secrets
        if: ${{ env.HAS_APP_CONFIG != '' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: worker
          wranglerVersion: "4.50.0"
          secrets: |
            GITHUB_OAUTH_CLIENT_ID
            GITHUB_OAUTH_CLIENT_SECRET
            GITHUB_APP_ID
            GITHUB_INSTALLATION_ID
            GITHUB_APP_PRIVATE_KEY
            SESSION_SECRET
        env:
          GITHUB_OAUTH_CLIENT_ID: ${{ secrets.OAUTH_CLIENT_ID }}
          GITHUB_OAUTH_CLIENT_SECRET: ${{ secrets.OAUTH_CLIENT_SECRET }}
          GITHUB_APP_ID: ${{ secrets.UPLOAD_APP_ID }}
          GITHUB_INSTALLATION_ID: ${{ secrets.UPLOAD_INSTALLATION_ID }}
          GITHUB_APP_PRIVATE_KEY: ${{ secrets.UPLOAD_APP_PRIVATE_KEY }}
          SESSION_SECRET: ${{ secrets.UPLOAD_SESSION_SECRET }}
